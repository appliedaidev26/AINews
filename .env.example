# =============================================================================
# DEVELOPMENT (local docker-compose / uvicorn)
# =============================================================================

DATABASE_URL=postgresql+asyncpg://ainews:ainews@localhost:5432/ainews
DATABASE_URL_SYNC=postgresql://ainews:ainews@localhost:5432/ainews

API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
ENVIRONMENT=development

# Firebase frontend (passed to Vite dev server via docker-compose)
VITE_FIREBASE_API_KEY=your_firebase_web_api_key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-firebase-project-id


# =============================================================================
# PRODUCTION (Cloud Run — set these as env vars in the Cloud Run service)
# =============================================================================

# DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@/DB?host=/cloudsql/PROJECT:REGION:INSTANCE
# DATABASE_URL_SYNC=postgresql://USER:PASSWORD@/DB?host=/cloudsql/PROJECT:REGION:INSTANCE
# CORS_ORIGINS=["https://your-app.web.app","https://your-app.firebaseapp.com"]
# ENVIRONMENT=production

# Frontend (baked in at build time — set in frontend/.env.local before npm run build)
# VITE_API_URL=https://your-cloud-run-service-url.run.app


# =============================================================================
# SHARED (same value in both environments)
# =============================================================================

# --- AI providers ---
# Get from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.5-flash
GEMINI_PRO_MODEL=gemini-2.5-pro
# Get from https://platform.openai.com/api-keys (optional fallback)
OPENAI_API_KEY=your_openai_api_key
# Get from https://console.anthropic.com (optional)
ANTHROPIC_API_KEY=your_anthropic_api_key

# --- xAI / Grok (https://console.x.ai — trending AI news via real-time X/Twitter search) ---
XAI_API_KEY=your_xai_api_key
XAI_MODEL=grok-4-fast-non-reasoning
XAI_QUERIES_PER_RUN=5
XAI_ARTICLES_PER_QUERY=15
XAI_BASE_ENGAGEMENT=120
XAI_MIN_ENGAGEMENT=1000

# --- Reddit (https://www.reddit.com/prefs/apps) ---
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
REDDIT_USER_AGENT=ainews/1.0

# --- Firebase backend (Project Settings → Service Accounts → Generate new private key) ---
FIREBASE_PROJECT_ID=your-firebase-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# --- Admin ---
ADMIN_API_KEY=generate_a_strong_random_key

# --- Ingestion tuning ---
HN_MIN_SCORE=50
ARXIV_MAX_RESULTS=30
DEDUP_SIMILARITY_THRESHOLD=0.85
